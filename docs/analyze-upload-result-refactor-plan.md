# /analyze 업로드·결과 플로우 리팩터링 제안 50선

| 번호 | 영역 | 리팩터링 제안 | 필요성 | 기대 효과 |
| --- | --- | --- | --- | --- |
| 1 | `src/pages/MobileAnalyzeStart.js` | 일일 사용량 한도를 `guestDailyQuota`/`memberDailyQuota` 상수 대신 설정 파일이나 API 응답으로 주입하도록 `useAuth` 훅을 확장해요. | 현재 10/20으로 하드코딩돼 있어 백엔드 정책 변경 시 릴리즈 없이는 수정이 불가능해요.【F:src/pages/MobileAnalyzeStart.js†L19-L23】 | 정책 변동에 대응하기 쉽고 환경별로 다른 한도를 손쉽게 적용할 수 있어요. |
| 2 | `src/pages/MobileAnalyzeStart.js` | `guidance`와 `guidance2`가 동일 문구를 번역 키만 다르게 공유하므로 하나의 컴포넌트/배열로 추출해 중복을 제거해요. | 같은 문장을 두 번 렌더링하면서 번역 키도 중복 관리해야 해요.【F:src/pages/MobileAnalyzeStart.js†L39-L46】 | 번역 키 관리 비용이 줄고 문구 변경 시 누락을 방지해요. |
| 3 | `src/pages/MobileAnalyzeStart.js` | 업로드 제한 안내(`Upload up to 5 images`)가 실제 업로드 제한(`MAX_IMAGE_FILES = 3`)과 다르므로 둘 중 하나를 상수로 공유하도록 리팩터링해요. | 사용자가 /upload 단계에서 다른 제한을 마주하면 혼란이 생겨요.【F:src/pages/MobileAnalyzeStart.js†L33-L36】【F:src/pages/MobileAnalyzeUpload.js†L12-L14】 | 일관된 안내로 이탈률을 줄이고 CS를 감소시켜요. |
| 4 | `src/pages/MobileAnalyzeStart.js` | `handleStart`를 `useCallback`으로 감싸고 의존성 배열을 명시해 재렌더 시 불필요한 함수 재생성을 줄여요. | 현재 렌더마다 새 함수가 생성돼 하위 컴포넌트 최적화를 방해해요.【F:src/pages/MobileAnalyzeStart.js†L28-L32】【F:src/pages/MobileAnalyzeStart.js†L118-L125】 | 메모이제이션된 버튼 컴포넌트에서 불필요한 리렌더를 줄일 수 있어요. |
| 5 | `src/pages/MobileAnalyzeStart.js` | CTA 및 카드 스타일 정의를 Tailwind preset 또는 공통 스타일 토큰으로 추출해 /upload, /result와 공유해요. | 동일 계열의 그라데이션/그림자 클래스가 여러 파일에 중복돼 있어 유지보수가 어려워요.【F:src/pages/MobileAnalyzeStart.js†L53-L116】 | 스타일 정책을 한 곳에서 조정할 수 있어 다국적 테마 전환 시 유리해요. |
| 6 | `src/pages/MobileAnalyzeStart.js` | `navigate(uploadRoute, { state: { sample: true } })` 호출을 커스텀 훅으로 캡슐화해 향후 샘플/무료 체험 플래그를 일관되게 전달해요. | 동일한 샘플 실행 로직이 다른 페이지에 분산될 경우 중복 위험이 있어요.【F:src/pages/MobileAnalyzeStart.js†L74-L94】 | 무료 체험 정책 변경 시 단일 훅만 수정하면 되어요. |
| 7 | `src/pages/MobileAnalyzeStart.js` | `useMemo`로 만든 `formatNumber`를 별도 유틸 함수(`useNumberFormatter`)로 옮겨 i18n 언어 변경 시 포맷 로직을 재사용해요. | 동일 포맷이 결과 페이지에서도 필요하지만 중복 구현돼요.【F:src/pages/MobileAnalyzeStart.js†L15-L24】 | 다국어 지원을 위한 숫자 포맷을 일관되게 재사용할 수 있어요. |
| 8 | `src/pages/MobileAnalyzeStart.js` | CTA 카드에 대한 분석 이력 보기 버튼을 TODO 주석 대신 Feature Flag 기반으로 토글하도록 정리해요. | 주석 처리된 버튼이 남아 있어 기능 상태 파악이 어려워요.【F:src/pages/MobileAnalyzeStart.js†L129-L137】 | 기능 토글이 명확해지고 Dead 코드가 제거돼 가독성이 좋아져요. |
| 9 | `src/pages/MobileAnalyzeStart.js` | `LifeBuoy` 지원 버튼의 네비게이션 경로를 상수(`supportRoute`) 대신 `useMemo` + `useCallback` 조합으로 래핑해요. | 현재 문자열 템플릿이 페이지마다 반복돼 관리가 어려워요.【F:src/pages/MobileAnalyzeStart.js†L26-L31】【F:src/pages/MobileAnalyzeStart.js†L107-L117】 | 라우트 구조 변경 시 수정 범위를 줄이고 테스트 코드를 작성하기 쉬워져요. |
| 10 | `src/pages/MobileAnalyzeStart.js` | `guestDailyQuota`와 `memberDailyQuota`를 타입 정의된 설정 객체로 노출해 스펙 문서(`/docs`)와 자동 동기화해요. | 현재 값 변경 시 문서와 코드가 따로 놀 위험이 있어요.【F:src/pages/MobileAnalyzeStart.js†L19-L23】 | 문서 자동화로 스펙/코드 괴리를 줄이고 협업 효율을 높여요. |
| 11 | `src/pages/MobileAnalyzeUpload.js` | 파일 제한/사이즈/허용 확장자 상수를 `features/analyze/constants.js`로 이동해 테스트와 다국어 안내에 재사용해요. | 여러 함수가 동일 상수를 직접 참조해 테스트가 어렵고 중복 안내가 발생해요.【F:src/pages/MobileAnalyzeUpload.js†L12-L27】 | 제한 변경 시 한 곳만 수정하면 되어 QA 속도가 빨라져요. |
| 12 | `src/pages/MobileAnalyzeUpload.js` | `isAllowedFile`가 이미지 외 타입에 대비하지 못하므로 백엔드 허용 확장자 목록을 fetch 후 동기화하도록 개선해요. | 프론트만 허용 목록을 고정하면 서버 허용 범위와 어긋날 수 있어요.【F:src/pages/MobileAnalyzeUpload.js†L15-L27】 | 서버 정책과 일치해 업로드 실패율을 줄이고 유지보수를 단순화해요. |
| 13 | `src/pages/MobileAnalyzeUpload.js` | `URL.createObjectURL`로 만든 프리뷰 URL을 Suspense-friendly 이미지 컴포넌트로 감싸 지연 로딩과 에러 처리 흐름을 통합해요. | 현재 단순 `<img>`로 로딩 실패/대체 텍스트 처리가 미흡해요.【F:src/pages/MobileAnalyzeUpload.js†L30-L59】 | 모바일 환경에서 프리뷰 실패 시 우아한 폴백을 제공해요. |
| 14 | `src/pages/MobileAnalyzeUpload.js` | `FileMetaCard` 삭제 버튼 SVG를 별도 아이콘 컴포넌트로 추출하고 `focus-visible` 스타일을 추가해 접근성을 높여요. | SVG가 인라인으로 중복되고 키보드 포커스 시 시각적 피드백이 없어요.【F:src/pages/MobileAnalyzeUpload.js†L40-L52】 | 키보드 사용자 경험을 향상하고 아이콘 재사용성을 확보해요. |
| 15 | `src/pages/MobileAnalyzeUpload.js` | `addFiles` 함수 내 중첩 로직을 `validateFile`/`dedupeFiles` 헬퍼로 분리하고 리듀서 패턴을 적용해 테스트 가능성을 높여요. | 현재 함수가 100라인 가까이 되어 복잡도가 높아요.【F:src/pages/MobileAnalyzeUpload.js†L86-L164】 | 단위 테스트 작성이 쉬워지고 버그 분석 시간이 단축돼요. |
| 16 | `src/pages/MobileAnalyzeUpload.js` | 파일 중복 검사를 `existKey` 대신 `Map`으로 추상화해 `lastModified`가 동일한 다른 파일을 구분하도록 개선해요. | `name+size+lastModified` 조합이 브라우저별로 일관되지 않을 수 있어요.【F:src/pages/MobileAnalyzeUpload.js†L104-L141】 | 교체 가능한 키 전략으로 중복 판별 정확도를 높여요. |
| 17 | `src/pages/MobileAnalyzeUpload.js` | 오류 메시지 배열(`msgs`)을 번역 키 중심으로 관리하고 렌더링 시에만 문자열 변환하도록 리팩터링해요. | 현재 즉시 문자열로 만들어 중복 번역을 발생시켜요.【F:src/pages/MobileAnalyzeUpload.js†L142-L175】 | 에러 메시지 국제화를 체계적으로 유지하고 테스트 시 문자열 비교를 단순화해요. |
| 18 | `src/pages/MobileAnalyzeUpload.js` | `handleAnalyze`에서 `stayOnPage` 플래그 대신 early return 구조로 제어 흐름을 단순화하고 로딩 상태 해제를 `finally`에서 일괄 처리해요. | 불필요한 상태 플래그 때문에 코드 이해가 어려워요.【F:src/pages/MobileAnalyzeUpload.js†L177-L236】 | 비동기 흐름을 예측하기 쉬워지고 로딩 상태 누락 위험이 줄어요. |
| 19 | `src/pages/MobileAnalyzeUpload.js` | `submitAnalyzeFiles` 호출 옵션(`buildMeta`)에서 동일 정보가 중복 전달되므로 업로드 단계에서 이미 생성한 메타를 재사용하도록 컨텍스트를 도입해요. | 파일 메타 작성이 업로드와 결과 단계에서 두 번 이뤄져요.【F:src/pages/MobileAnalyzeUpload.js†L187-L212】【F:src/features/analyze/api/submitAnalyze.js†L59-L84】 | 메타 정보 관리가 단순화되고 추후 로깅/추적 코드도 재사용할 수 있어요. |
| 20 | `src/pages/MobileAnalyzeUpload.js` | 모델 목록 fetch에 `AbortController`를 사용해 빠른 네비게이션 시 메모리 누수를 방지해요. | 현재 `aborted` 플래그만 사용해도 fetch 응답이 완료되면 setState가 발생할 수 있어요.【F:src/pages/MobileAnalyzeUpload.js†L64-L111】 | 컴포넌트 언마운트 시 안전하게 요청을 취소해 리소스를 절약해요. |
| 21 | `src/pages/MobileAnalyzeUpload.js` | 모델 선택 UI를 `useMemo`로 메모이즈된 JSX 조각으로 분리하고 Skeleton 컴포넌트를 도입해 로딩 상태를 시각적으로 명확히 해요. | 현재 조건부 렌더링이 길고 상태별 스타일이 섞여 가독성이 떨어져요.【F:src/pages/MobileAnalyzeUpload.js†L238-L283】 | 코드 구조가 단순해지고 사용자에게 명확한 로딩 피드백을 제공해요. |
| 22 | `src/pages/MobileAnalyzeUpload.js` | 업로드 버튼과 분석 버튼에 대해 `aria-busy`와 `aria-disabled` 속성을 추가해 스크린리더 접근성을 개선해요. | 현재는 `disabled`만 설정돼 있어 상태 전달이 제한적이에요.【F:src/pages/MobileAnalyzeUpload.js†L316-L361】 | 보조공학 사용자가 현재 상태를 정확히 파악할 수 있어요. |
| 23 | `src/pages/MobileAnalyzeUpload.js` | 오류 모달 트리거 시 `pendingResultPath`를 상태 대신 라우터 검색 파라미터로 전달해 새로고침에도 이어보기가 가능하도록 해요. | 현재 새로고침 시 대기 중이던 결과 페이지로 이동할 수 없어요.【F:src/pages/MobileAnalyzeUpload.js†L214-L236】 | 사용자가 네트워크 문제 후에도 결과 페이지에 쉽게 접근할 수 있어요. |
| 24 | `src/pages/MobileAnalyzeUpload.js` | `albumInputRef`를 커스텀 훅(`useHiddenFileInput`)으로 추출해 데스크톱 업로드 페이지와 재사용하도록 해요. | 숨겨진 파일 입력 조작 로직이 다른 페이지에도 필요할 수 있어요.【F:src/pages/MobileAnalyzeUpload.js†L167-L176】【F:src/pages/MobileAnalyzeUpload.js†L366-L381】 | 업로드 컴포넌트 전반에 동일한 UX를 손쉽게 적용할 수 있어요. |
| 25 | `src/pages/MobileAnalyzeUpload.js` | 업로드 목록이 많아질 때를 대비해 `FileMetaCard` 리스트를 `React.memo` 또는 가상 스크롤로 최적화해요. | 현재 모든 카드가 매 렌더마다 다시 그려져요.【F:src/pages/MobileAnalyzeUpload.js†L286-L307】 | 대량 업로드 시에도 스크롤이 부드럽고 배터리 사용량을 줄여요. |
| 26 | `src/pages/MobileAnalyzeUpload.js` | 업로드 진행 시 토스트/스낵바와 오류 모달을 통합 관리하는 상태 관리 훅을 도입해 피드백을 일관되게 제공해요. | 에러만 모달로 노출되고 성공 피드백이 없어 UX가 단절돼요.【F:src/pages/MobileAnalyzeUpload.js†L214-L257】 | 사용자에게 단계별 피드백을 제공해 이탈을 줄여요. |
| 27 | `src/features/analyze/api/submitAnalyze.js` | `fetch` 반복 호출을 `Promise.allSettled`와 큐잉 전략으로 변환해 병렬 업로드와 API 폭주를 제어해요. | 현재 순차적으로 업로드/생성이 이뤄져 대기 시간이 길어요.【F:src/features/analyze/api/submitAnalyze.js†L24-L89】 | 업로드 처리량이 향상되고 사용자 체감 속도가 빨라져요. |
| 28 | `src/features/analyze/api/submitAnalyze.js` | 업로드·분석 요청 실패 시 재시도 로직과 백오프를 적용할 수 있도록 옵션을 추가해요. | 일시적 네트워크 오류에도 즉시 실패해 사용자 재시도가 필요해요.【F:src/features/analyze/api/submitAnalyze.js†L36-L83】 | 자동 재시도를 통해 성공률을 높이고 CS 부담을 줄여요. |
| 29 | `src/features/analyze/api/submitAnalyze.js` | `sessionStorage` 접근 전 브라우저 환경 여부를 체크하는 가드 함수를 두어 SSR/프리렌더 환경 오류를 방지해요. | 현재 `sessionStorage`가 없는 환경에서 ReferenceError가 발생해요.【F:src/features/analyze/api/submitAnalyze.js†L63-L86】 | 서버 렌더링 지원 시도 시 런타임 오류를 예방해요. |
| 30 | `src/features/analyze/api/submitAnalyze.js` | 업로드/분석 응답 스키마에 대한 타입 정의(예: TypeScript 선언 또는 JSDoc)를 추가해 API 변경 감지 테스트를 자동화해요. | 현재 응답 구조가 암묵적으로만 사용돼 변경 시 버그 위험이 커요.【F:src/features/analyze/api/submitAnalyze.js†L24-L87】 | 정적 분석으로 조기 경고를 받아 품질을 높일 수 있어요. |
| 31 | `src/features/analyze/api/submitAnalyze.js` | 파일 루프에서 `for...of` 대신 `for await` + `async` 제너레이터를 사용해 취소 가능한 업로드 파이프라인을 구현해요. | 중간에 사용자가 업로드를 취소해도 이미 시작한 요청을 중단할 수 없어요.【F:src/features/analyze/api/submitAnalyze.js†L24-L89】 | 업로드 취소/재시작 UX를 제공해 모바일 데이터 사용량을 줄여요. |
| 32 | `src/pages/MobileAnalyzeResult.js` | `sessionStorage`에서 토큰을 읽은 뒤 즉시 삭제하지 않고 `keepAlive` 옵션을 두어 새 탭에서 복구 가능하도록 해요. | 현재 탭을 닫으면 결과 복원이 불가능해요.【F:src/pages/MobileAnalyzeResult.js†L40-L83】 | 사용자가 실수로 뒤로 가더라도 다시 결과를 조회할 수 있어요. |
| 33 | `src/pages/MobileAnalyzeResult.js` | SSE 연결에서 `es.onerror` 시 자동 재연결 로직과 최대 재시도 횟수를 설정해 일시적 네트워크 문제에 대응해요. | 현재 한 번 에러가 나면 연결이 끊기고 재시도하지 않아요.【F:src/pages/MobileAnalyzeResult.js†L72-L108】 | 모바일 환경의 끊김에도 결과 수집을 지속할 수 있어요. |
| 34 | `src/pages/MobileAnalyzeResult.js` | `reports` 상태를 `useReducer`로 전환해 `connected`, `stage`, `result` 업데이트 액션을 명확히 분리해요. | 동일 객체를 매번 전개해 setState하면서 버그 추적이 어려워요.【F:src/pages/MobileAnalyzeResult.js†L34-L123】 | 상태 전이를 추적하기 쉬워 디버깅/테스트가 용이해요. |
| 35 | `src/pages/MobileAnalyzeResult.js` | URL 파라미터 파싱을 커스텀 훅(`useJobIds`)으로 추출하고 검증 실패 시 사용자 피드백을 노출해요. | 현재 비어 있는 `jobIds`만 처리하고 기타 오류는 조용히 무시돼요.【F:src/pages/MobileAnalyzeResult.js†L16-L32】 | 잘못된 링크 공유 시 사용자에게 명확한 안내를 제공해요. |
| 36 | `src/pages/MobileAnalyzeResult.js` | `stageOrder`를 서버 단계(enum)와 동기화된 상수 모듈에서 불러오도록 리팩터링해요. | 프론트에서 ALIGN/INFER/POST 순서를 고정해 변경 시 분기 오류가 생겨요.【F:src/pages/MobileAnalyzeResult.js†L124-L137】 | 백엔드 단계 추가 시 빠르게 대응할 수 있어요. |
| 37 | `src/pages/MobileAnalyzeResult.js` | 진행률(`progressWidth`) 계산을 헬퍼 함수로 추출하고 NaN 처리·로그를 추가해요. | 현재 `entry.progress`가 문자열이면 조용히 NaN을 반환해요.【F:src/pages/MobileAnalyzeResult.js†L148-L165】 | 데이터 오류 조기 감지 및 로깅이 가능해요. |
| 38 | `src/pages/MobileAnalyzeResult.js` | 실패 카드와 성공 카드 UI를 별도 컴포넌트로 분리하고 Suspense 경계 안에서 로딩 스켈레톤을 제공해요. | JSX가 200라인 가까이 연속돼 읽기 어렵고 테스트가 힘들어요.【F:src/pages/MobileAnalyzeResult.js†L138-L236】 | 컴포넌트 단위 테스트가 가능해지고 유지보수성이 향상돼요. |
| 39 | `src/pages/MobileAnalyzeResult.js` | `AnimatePresence` 사용 시 `layout` prop을 공통 래퍼에 옮겨 애니메이션 정의를 재사용해요. | 현재 각 섹션마다 동일 transition이 반복돼요.【F:src/pages/MobileAnalyzeResult.js†L153-L209】 | 애니메이션 변경 시 수정 범위를 줄이고 일관성을 확보해요. |
| 40 | `src/pages/MobileAnalyzeResult.js` | `hasPending`/`hasResults` 계산을 `useMemo`로 래핑해 반복 계산을 줄이고 테스트 시 명시적으로 검증해요. | 현재 렌더마다 `analysisStates.some`가 호출돼 비용이 발생해요.【F:src/pages/MobileAnalyzeResult.js†L132-L137】 | 대량 작업 시 성능을 최적화하고 테스트에서 의도를 명확히 할 수 있어요. |
| 41 | `src/features/analyze/components/mobile/MobileAnalysisReport.jsx` | `LABEL_TONE`을 디자인 토큰 파일로 이동해 결과/오류 카드가 동일 팔레트를 공유하도록 해요. | 페이지마다 유사한 색상 정의가 중복돼요.【F:src/features/analyze/components/mobile/MobileAnalysisReport.jsx†L3-L21】 | 다크 모드/브랜드 색상 변경 시 유지보수가 쉬워요. |
| 42 | `src/features/analyze/components/mobile/MobileAnalysisReport.jsx` | `toPercent` 함수에 로케일 기반 포맷을 도입하고 소수 자릿수 정책을 상수로 관리해요. | 현재 고정 한글/영문 혼합 환경에서 소수점 표시가 일관되지 않아요.【F:src/features/analyze/components/mobile/MobileAnalysisReport.jsx†L23-L34】 | 언어별 포맷 차이를 반영해 가독성을 높여요. |
| 43 | `src/features/analyze/components/mobile/MobileAnalysisReport.jsx` | `summaryLines` 생성 로직을 유닛 테스트 가능한 유틸(`buildReportSummary`)로 분리해 임계값 변경 시 회귀를 방지해요. | 조건 분기와 수치 비교가 컴포넌트 안에 섞여 있어 테스트하기 어렵어요.【F:src/features/analyze/components/mobile/MobileAnalysisReport.jsx†L46-L67】 | 분석 요약 문구가 정책 변경에도 안정적으로 유지돼요. |
| 44 | `src/features/analyze/components/mobile/MobileAnalysisReport.jsx` | `heroImage` base64 처리 시 이미지 크기 최적화를 위해 `URL.createObjectURL` 또는 CDN 썸네일링을 도입해요. | 대형 base64가 그대로 DOM에 삽입되면 메모리 사용량이 증가해요.【F:src/features/analyze/components/mobile/MobileAnalysisReport.jsx†L35-L44】 | 모바일 메모리를 절약하고 로딩 속도를 개선해요. |
| 45 | `src/features/analyze/components/mobile/MobileAnalysisReport.jsx` | 메트릭 카드(`MetricRow`)를 공통 컴포넌트로 추출해 결과 페이지 및 향후 데스크톱 뷰에서도 재사용해요. | 동일한 스타일 구조가 다른 컴포넌트에도 필요하지만 중복 정의돼요.【F:src/features/analyze/components/mobile/MobileAnalysisReport.jsx†L56-L83】 | 디자인 일관성을 확보하고 컴포넌트 라이브러리화를 촉진해요. |
| 46 | `src/features/analyze/components/mobile/MobileAnalysisReport.jsx` | 결과 ID 표시(`jobId.slice(-6)`) 로직을 헬퍼로 이동하고 마스킹 정책을 도입해 개인정보 노출을 최소화해요. | 현재 ID 형식이 바뀌면 컴포넌트에서 직접 수정해야 해요.【F:src/features/analyze/components/mobile/MobileAnalysisReport.jsx†L49-L54】 | ID 포맷 변경을 중앙집중식으로 제어할 수 있어요. |
| 47 | `src/features/analyze/components/ErrorModal.jsx` | `messages` 렌더링 시 HTML 이스케이프를 보장하는 헬퍼를 사용해 서버 메시지 삽입 시 XSS를 방지해요. | 현재 문자열을 그대로 `<li>`에 삽입해 잠재적 위험이 있어요.【F:src/features/analyze/components/ErrorModal.jsx†L59-L99】 | 외부 입력에 안전한 에러 다이얼로그를 제공해요. |
| 48 | `src/features/analyze/components/ErrorModal.jsx` | `lockScroll` 기본값을 `document.body.dataset` 기반 전역 관리로 전환해 모달이 여러 개 열릴 때 스크롤 잠금 충돌을 방지해요. | 다중 모달 사용 시 마지막 모달이 닫혀도 스크롤이 잠긴 채 남을 수 있어요.【F:src/features/analyze/components/ErrorModal.jsx†L27-L53】 | 모달 스택을 안정적으로 제어해 UX를 개선해요. |
| 49 | `src/api/endPointRoute.js` (간접) | `ANALYZE_MODEL_ENDPOINTS`와 `ANALYZE_ENDPOINTS` URL을 환경별로 주입하고 타입 검증을 추가해 잘못된 구성 시 조기 경고해요. | 현재 문자열 상수가 유효한지 확인할 방법이 없어요.【F:src/pages/MobileAnalyzeUpload.js†L8-L10】【F:src/pages/MobileAnalyzeResult.js†L8-L9】 | 배포 환경 설정 오류를 미리 잡아 서비스 장애를 예방해요. |
| 50 | 전체 `/analyze` 라우트 | 업로드→결과 플로우에서 사용하는 상태를 `AnalyzeFlowProvider`로 묶어 라우팅 간 데이터 이동을 단순화해요. | 현재 페이지마다 `useNavigate`/`sessionStorage`로 상태를 전달해 코드가 분산돼요.【F:src/pages/MobileAnalyzeUpload.js†L179-L236】【F:src/pages/MobileAnalyzeResult.js†L16-L123】 | 다단계 플로우 확장 시 상태 전달이 쉬워지고 테스트 가능성이 높아져요. |

